<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DatHang" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- Màu thương hiệu chính -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_dark_blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Màu phụ (accent) -->
        <item name="colorSecondary">@color/accent_orange</item>
        <item name="colorSecondaryVariant">@color/accent_orange</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Màu thanh trạng thái -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary_dark_blue</item>

        <!-- Bạn có thể thêm màu nền nếu muốn giao diện sáng -->
        <!-- <item name="android:windowBackground">@color/background_light</item> -->

        <!-- Customize your theme here. -->
    </style>
</resources>
